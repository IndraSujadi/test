<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class student extends Model
{
    //
    // dipakai kalau nama tabel tidak default ( contoh tidak default mahasiswa), contoh default(kata plural bhs inggris 
    // studentc, doctors, employees, dll)
    //protected $table = 'mahasiswa';
}
